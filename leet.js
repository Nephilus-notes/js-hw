// return a single string based on the value of n

function fizzbuzz (n) {
    if (n % 5 === 0 && n % 3 === 0) {
        return "Fizzbuzz"
    } else if (n % 5 === 0) {
        return "Buzz"
    } else if (n % 3 === 0) {
        return "Fizz"
     } else {
        return N
     }
}